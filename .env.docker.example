# Copy to .env for docker compose (optional overrides)
OLLAMA_MODEL=llama3.2
OLLAMA_VISION_MODEL=llava

# Pull a vision model on the host before using OCR:
#   ollama pull llava
# Or: ollama pull llama3.2-vision
